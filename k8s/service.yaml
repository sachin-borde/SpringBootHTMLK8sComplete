apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  type: NodePort
  sessionAffinity: ClientIP  # For sticky sessions
  selector:
    app: spring-boot-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 31000  # Valid range (30000-32767)